{
  "name": "countdown",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "gh-pages": "^0.12.0"
  },
  "scripts": {
    "postinstall": "npm run build && open index.html",
    "build": "./node_modules/.bin/babel app.js -o app-compiled.js && sh build.sh",
    "deploy": "npm run build && ./node_modules/.bin/gh-pages -d dist"
  }
}
